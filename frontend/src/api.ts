export const api=async<T>(path:string,init?:RequestInit):Promise<T>=>{const response=await fetch(path,{headers:{'Content-Type':'application/json',...(init?.headers||{})},...init});const data=await response.json();if(!response.ok)throw new Error(data.detail||data.error?.message||'Request failed');return data};
export type Dataset={id:string;name:string;display_name:string;table:string;description:string;source:string;license:string;rows:number;columns:number;tasks:string[];targets:string[];version:string;parent_id?:string};
export type Step={tool:string;label:string;status?:string;duration_ms?:number;result?:any;error?:string};
export type AnalystResult={session_id:string;run_id:string;status:string;answer?:string;answer_structure?:{headline:string;metrics:{label:string;value:string}[];sections:{title:string;items:string[]}[];note?:string};plan:Step[];steps:Step[];trace:any[];artifacts:any[];experiments:any[];errors:any[];pending_approval?:any;context:{dataset_id?:string;target?:string;task_type?:string}};
