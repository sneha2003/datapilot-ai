import React from 'react'; import ReactDOM from 'react-dom/client'; import {BrowserRouter} from 'react-router-dom'; import {QueryClient,QueryClientProvider} from '@tanstack/react-query'; import App from './App'; import './styles.css';
const client=new QueryClient({defaultOptions:{queries:{retry:1,staleTime:15000}}});
class ErrorBoundary extends React.Component<React.PropsWithChildren,{error?:Error}>{state:{error?:Error}={};static getDerivedStateFromError(error:Error){return {error}}render(){return this.state.error?<div className="grid min-h-screen place-items-center bg-slate-50 p-8"><div className="max-w-3xl rounded-xl border border-red-200 bg-white p-6 shadow-sm"><div className="font-semibold text-slate-900">This view could not be displayed</div><pre className="mt-2 whitespace-pre-wrap text-xs text-slate-600">{this.state.error.stack||this.state.error.message}</pre><button className="mt-4 rounded-lg bg-slate-900 px-4 py-2 text-sm text-white" onClick={()=>location.reload()}>Reload workspace</button></div></div>:this.props.children}}
ReactDOM.createRoot(document.getElementById('root')!).render(<React.StrictMode><ErrorBoundary><QueryClientProvider client={client}><BrowserRouter><App/></BrowserRouter></QueryClientProvider></ErrorBoundary></React.StrictMode>);
